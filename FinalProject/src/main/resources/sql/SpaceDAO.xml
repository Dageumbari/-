<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTDEMapper3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.model.dao.SpaceDAO">


	<!-- <resultMap id="draftCardListMap" type="draftCardList">
		<collection property="draftCards" javaType="List"
			column="draftCode" ofType="DraftCardList" select="getdraftList">
		</collection> 
	</resultMap>-->
	<!-- space -->
	<!-- <resultMap type="com.bit.model.dto.SpaceDTO" id="space_resultMap"> 
		<id property="spaceCode" column="spaceCode" /> <result property="userNo" 
		column="userNo" /> <result property="spaceName" column="spaceName" /> <result 
		property="spaceUrl" column="spaceUrl" /> <collection property="draftList" 
		column="draftCode" ofType="draftList" select="selectDraftsForSpace"> </collection> 
		</resultMap> -->
<!-- 
	<resultMap type="com.bit.model.dto.space.DraftListDTO"
		id="draftList_resultMap">
		<id property="spaceCode" column="spaceCode" />
		<collection property="draftDTO" column="draftCode"
			ofType="DraftDTO" select="getDraftList">
			
		</collection>
	</resultMap>
 -->



	<!-- <result property="draftStatus" column="draftStatus" typeHandler=""/> -->


	<!-- 드래프트 리스트 출력 -->
	<!-- <select id="getdraftList" resultMap="draftCardListMap"> 
	<select id="getdraftList" >
		select * from draftCardList
	</select>
-->
	<!-- 드래프트 카드 디테일 -->
	<!-- <select id="getDraftCard">
		select * from draftCard
	</select>

	<resultMap type="pageGroupDTO" id="pageGroup_resultMap">
	</resultMap>

	<select id="getSpaceUserInfo" resultType="spaceUserInfoDTO">
		select
		spaceCode,spaceName,spaceUrl,name,userNo,userImage from spaceUserInfo
		where userNo=#{userNo} and spaceCode=#{spaceCode}
	</select>


	드래프트 메시지 입력, 저장
	<insert id="insertDraftMsg" parameterType="Map">
		insert into
		draftMsg
		values (draftMsg_seq.nextval), #{msgContent}, #{editsCount},
		#{draftCode} )
	</insert> -->
	
	<!-- <select id="getDraftList" resultMap="draftList_resultMap">
		select * from draft
	</select> -->

	<!-- <resultMap type="draftDTO" id="draft_resultMap">
		<id property="draftCode" column="draftCode" />
		<result property="draftSavetime" column="draftSaveTime" />
		<result property="draftStatus" column="draftStatus" />
	</resultMap> -->

	<update id="updateDraftMsg" parameterMap="String">
		update draftMsg set
		msgContent=#{msgContent}
		where draftMsgno=#{draftMsgNo}
	</update>

	<!-- 드래프트 카드 생성 -->


	<!-- sidenav header의 사용자 로고이미지, url은 툴팁때문에 넣음 -->
	<!-- <select id="com.bit.model.dto.space.getUserInfo" resultMap="space_resultMap"> 
		select userNo, userImage, userUrl from userInfo where userNo=#{userNo} </select> -->



</mapper>