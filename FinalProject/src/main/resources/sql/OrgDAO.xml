<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTDEMapper3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.model.dao.OrgDAO">
	<select id="getAllOrgList" >
		SELECT *
			FROM dashBoard
		WHERE 
	</select>
	<select id="getOrgInfo" >
		SELECT dashboardimage, dashboardname
			FROM dashBoard
		WHERE dashBoardCode = #{dashboardCode}
	</select>
	<delete id="deleteOrg">
		DELETE *
			FROM dashBoard
		WHERE dashboardCode = #{dashboardCode}
	</delete>
	<insert id="createOrg">
		INSERT
			FROM
		WHERE
	</insert>
	<update id="orgNameModify">
		UPDATE
			FROM
		WHERE
	</update>
	<update id="orgOrderModify">
		UPDATE 
			FROM
		WHERE 
	</update>
	<!-- 
	<update id="orgLogoModify">
		UPDATE
			FROM
		WHERE
	</update>
	 -->
</mapper>